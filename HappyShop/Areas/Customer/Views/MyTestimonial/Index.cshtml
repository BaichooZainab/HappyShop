@* @model IEnumerable<Testimonial>

@{
    ViewData["Title"] = "Index";
}

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-dark bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Customer's Testimonials</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Testimonial" asp-action="Create"
                   class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> &nbsp; Testify Here
                </a>
            </div>
        </div>
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>

                <tr>
                    <th>Customer Name</th>
                    <th>Testimonials</th>
                    <th>Image</th>
                    <th>Date Posted</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="50%">
                            @obj.Name
                        </td>
                        <td width="30%">
                            @obj.Testimonials
                        </td>
                        <td>
                            @if (!string.IsNullOrEmpty(obj.Image))
                            {
                                <img src="@Url.Content(obj.Image)" alt="Product Image" style="max-width: 100px; max-height: 100px;" />
                            }

                        </td>
                        <td width="30%">
                            @obj.Date.ToString("yyy-MM-dd")
                        </td>
                        <td>
                            <div class="w-75 btn-group" role="group">


                                <a asp-controller="MyTestimonial" asp-action="Edit"
                                   asp-route-id="@obj.Id" class="btn btn-warning mx-2">
                                    <i class="bi bi-pencil-square"></i>Edit
                                </a>

                                <a asp-controller="MyTestimonial" asp-action="Delete"
                                   asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash3"></i>Delete
                                </a>

                               

                            </div>

                        </td>



                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

 *@



@model IEnumerable<Testimonial>

@{
    ViewData["Title"] = "Customer Testimonials";
}

<style>
    .testimonial-card:hover {
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        transform: translateY(-2px);
    }
</style>

<section class="testimonials">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 mx-auto">
                <div class="card border-0 mt-4">
                    <div class="card-header bg-dark text-white">
                        <h2 class="text-center">Customer Testimonials</h2>
                    </div>
                    <div class="card-body">
                        <div class="row mb-3">
                            <div class="col-6"></div>
                            <div class="col-6 text-end">
                                <a asp-controller="MyTestimonial" asp-action="Create" class="btn btn-primary">
                                    <i class="bi bi-plus-circle"></i> Testify Here
                                </a>
                            </div>
                        </div>
                        <div class="row">
                            @foreach (var obj in Model)
                            {
                                <div class="col-md-4 mb-4">
                                    <div class="card testimonial-card">
                                        <img src="@Url.Content(obj.Image)" class="card-img-top" alt="Customer Image">
                                        <div class="card-body">
                                            <h5 class="card-title">@obj.Name</h5>
                                            <p class="card-text">@obj.Testimonials</p>
                                            <p class="card-text"><small class="text-muted">@obj.Date.ToString("yyy-MM-dd")</small></p>
                                            <div class="btn-group" role="group">
                                                <a asp-controller="MyTestimonial" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-warning">
                                                    <i class="bi bi-pencil-square"></i> Edit
                                                </a>
                                                <a asp-controller="MyTestimonial" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger">
                                                    <i class="bi bi-trash3"></i> Delete
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
